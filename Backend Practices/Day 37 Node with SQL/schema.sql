-- CREATE TABLE user (
--     id VARCHAR(50) PRIMARY KEY,
--     username VARCHAR(50) UNIQUE NOT NULL,
--     email VARCHAR(50) NOT NULL,
--     password VARCHAR(50) NOT NULL
-- );

-- -- SELECT * FROM user;

INSERT INTO user
(id, username, email, password)
VALUES
("puppi_", "Puppi", "puppi@gmail.com", "pup123@");