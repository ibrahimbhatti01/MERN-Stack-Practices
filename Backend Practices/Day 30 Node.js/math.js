// Export a function that returns the sum of two numbers
export const sum = (a, b) => a + b;

// Export a function that returns the product of two numbers
export const mul = (a, b) => a * b;

// Export the constant PI (approximation of the mathematical constant π)
export const PI = 3.14;

// Export the constant g (acceleration due to gravity in m/s²)
export const g = 9.8;
